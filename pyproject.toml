[project]
name = "fastbrowse"
version = "0.3.0"
description = "A browser agent that picks instead of generating: Jev chooses, an LLM reads and plans, code verifies."
readme = "README.md"
license = "MIT"
license-files = ["LICENSE", "NOTICE"]
authors = [{ name = "Agent Labs" }]
requires-python = ">=3.14"
dependencies = [
    "cdp-use==1.4.5",
    "httpx[http2]>=0.28,<1",
    "pydantic>=2.11,<3",
    "pydantic-settings>=2.10,<3",
]

[project.urls]
Homepage = "https://github.com/agent-labs-dev/fastbrowse"
Repository = "https://github.com/agent-labs-dev/fastbrowse"
Issues = "https://github.com/agent-labs-dev/fastbrowse/issues"

[project.optional-dependencies]
browser-use = ["browser-use-sdk>=3.11.3,<3.12"]
# The MCP server, `fastbrowse-mcp`.
mcp = ["mcp>=1.20,<2"]

[project.scripts]
fastbrowse = "fastbrowse.cli:main"
fastbrowse-mcp = "fastbrowse.mcp_server:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fastbrowse"]

[dependency-groups]
dev = [
    "pytest>=8.4,<9",
    "pytest-asyncio>=1,<2",
    "ruff>=0.14,<1",
    "pyright>=1.1.400",
    "vale==3.22.0.0",
    "pytest-timeout>=2.3,<3",
]

[tool.ruff]
line-length = 120
target-version = "py314"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM", "RUF", "ASYNC"]

[tool.pytest.ini_options]
testpaths = ["tests"]
# Browser tests import helpers as tests.browser.*, which needs the repo root importable.
pythonpath = ["."]
asyncio_mode = "auto"
# A hung browser call otherwise holds CI for its full hour with no output; this fails the test and dumps every stack.
timeout = 120

[tool.pyright]
include = ["src", "tests"]
pythonVersion = "3.14"
typeCheckingMode = "strict"
